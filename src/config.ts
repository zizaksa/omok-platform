import * as path from 'path';

export const APP_CONFIG = {
    CLIENT_HOST: '127.0.0.1',
    CLIENT_PORT: 8080,
    SERVER_HOST: '127.0.0.1',
    SERVER_PORT: 8081,
    AI_DIRPATH: path.join(__dirname, '..', 'ai')
};
